import React from "react";

export default function CompanyHeader({
  quote,
  stock,
  low52,
  high52,
  watchlist,
  compareList,
  toggleWatch,
  toggleCompare,
  setTab,
  fmtNum,
  fmtCr,
  Move,
  LiveTag,
  IconBtn,
  Bookmark,
  Layers,
  FileText,
  THEME,
}) {
  return (
    <Panel style={{ padding: 20, marginBottom: 18 }}>
      {/* Header JSX will be moved here in the next step */}
    </Panel>
  );
}